function encontreMax(n) {
  if (let = valores % 2 === 0) {
    console.log(`Os valores ${valores} são pares`);
  } else {
    console.log(`Os valores ${valores} são impares`);
  }
}

const valor = [2];
encontreMax(valor);
